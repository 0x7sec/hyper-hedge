# KuCoin Earn Bot — Dependencies
# Core (already in requirements.txt for main bot):
numpy>=1.24
requests>=2.28
rich>=13.0

# Optional (for chart generation — will be skipped if unavailable):
# matplotlib>=3.7
# pandas>=1.5
